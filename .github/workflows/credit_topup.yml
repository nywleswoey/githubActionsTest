name: Credit Top-up Pipeline

on:
  workflow_dispatch:

env:
  DOCKERFILE: "${{ GITHUB_WORKSPACE }}/cmd/credit-topup/Dockerfile"

jobs:
  build-and-push:
    name: Build and Push
    steps:
      - name: Show values
        run: echo "$DOCKERFILE"